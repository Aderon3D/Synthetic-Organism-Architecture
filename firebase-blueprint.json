{
  "entities": {
    "EpisodicMemory": {
      "title": "EpisodicMemory",
      "description": "A single episodic memory of the organism.",
      "type": "object",
      "properties": {
        "id": {
          "type": "number",
          "description": "Timestamp ID of the memory"
        },
        "time": {
          "type": "number",
          "description": "Simulation time when the memory was formed"
        },
        "stimulus": {
          "type": "string",
          "description": "The sensory input or event"
        },
        "outcome": {
          "type": "string",
          "description": "The result or thought associated with the stimulus"
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this organism"
        }
      },
      "required": ["id", "time", "stimulus", "outcome", "userId"]
    },
    "OrganismState": {
      "title": "OrganismState",
      "description": "The persistent state of the organism.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this organism"
        },
        "energy": {
          "type": "number"
        },
        "freeEnergy": {
          "type": "number"
        },
        "boredom": {
          "type": "number"
        },
        "linguisticStyle": {
          "type": "string"
        },
        "updatedAt": {
          "type": "number"
        }
      },
      "required": ["userId", "energy", "freeEnergy", "boredom", "linguisticStyle", "updatedAt"]
    }
  },
  "firestore": {
    "/users/{userId}/memories/{memoryId}": {
      "schema": "EpisodicMemory",
      "description": "Episodic memories of the organism for a specific user."
    },
    "/users/{userId}/state/current": {
      "schema": "OrganismState",
      "description": "The current persistent state of the organism."
    }
  }
}
